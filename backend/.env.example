SECRET_KEY=supersecretkey1234567890123456789012
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7
# --- Database Connection ---
# Neon PostgreSQL (copy from Neon Dashboard -> Connection String -> Python/SQLAlchemy):
# DATABASE_URL=postgresql://<user>:<password>@ep-<your-project>.<region>.aws.neon.tech/neondb
# Local PostgreSQL (default fallback):
DATABASE_URL=postgresql+psycopg2://postgres:tanmay%40123@localhost:5432/smart_url_shortener_db
# Set to 0 only for localhost Postgres without SSL. Neon and other managed DBs require SSL.
DB_SSL_REQUIRE=1
BACKEND_BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
AUTO_CREATE_TABLES=true
ADMIN_EMAILS=
